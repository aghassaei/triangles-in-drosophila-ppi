#main program
from utils import*
def main():
    # Read in example
    node_list, edge_list= read_in('example.txt')
    print(node_list)
    
    print(edge_list)
    
    # read in
    # make function that takes a single node and returns list of tuples with other
    # nodes in that triangle
    # make function that does this for all nodes
        # return that 
    return 0


if __name__ == '__main__':
    main()